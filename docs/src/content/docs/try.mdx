---
title: Live session
description: ilrepl running in your browser on the .NET WebAssembly runtime.
tableOfContents: false
---

This is ilrepl itself, compiled to WebAssembly. Cells compile with Reflection.Emit on the Mono interpreter. Nothing leaves your browser. The runtime is about 15 MB, so the first load takes a moment.

<div id="session" class="not-content">
  <div id="session-header">
    <div class="dots"><span class="dot r"></span><span class="dot y"></span><span class="dot g"></span></div>
    <span class="title">ilrepl</span>
    <span class="status" id="session-status">Loading</span>
  </div>
  <div id="terminal"></div>
</div>

Click into the terminal and type. Tab completes opcodes, Up and Down walk history, `.help` lists the commands.

```
ldc.i4 6
ldc.i4 7
mul
ret
```

<script is:inline src="/ilrepl/try/main.js"></script>
